# Campus Marketplace API

FastAPI + SQLModel backend for the Campus Marketplace.

```bash
poetry install
poetry run python -m app.seed        # optional demo data
poetry run uvicorn app.main:app --reload --port 8000
```

Interactive docs at `http://localhost:8000/docs`.
